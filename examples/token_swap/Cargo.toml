[package]
name = "token_swap"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "2.2.0"
spl-token = { version = "4.0", features = ["no-entrypoint"] }

[dev-dependencies]
litesvm = "0.6.1"
solana-sdk = "2.2.0"
